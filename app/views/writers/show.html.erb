<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @writer.name %>
</p>

<%= form_for [@writer, @writer.comments.build] do |f| %>
  <%= f.text_area :body, rows: 5 %><br>
  <%= f.submit 'Send' %>
<% end %>

<%= link_to 'Edit', edit_writer_path(@writer) %> |
<%= link_to 'Back', writers_path %>
